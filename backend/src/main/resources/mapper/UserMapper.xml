<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jiepaiqi.identity.UserMapper">
    <insert id="insert" parameterType="com.jiepaiqi.identity.User">
        INSERT INTO users (id, username, display_name, role, created_at)
        VALUES (#{id}, #{username}, #{displayName}, #{role}, #{createdAt})
    </insert>

    <update id="update" parameterType="com.jiepaiqi.identity.User">
        UPDATE users
        SET username = #{username},
            display_name = #{displayName},
            role = #{role}
        WHERE id = #{id}
    </update>
</mapper>